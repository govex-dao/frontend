[build]
buildCommand = "pnpm install --frozen-lockfile --prod=false && VITE_NETWORK=${VITE_NETWORK:-mainnet} VITE_BACKEND_URL=${VITE_BACKEND_URL:-https://backend-api-v2-mainnet.up.railway.app} pnpm build"

[deploy]
startCommand = "pnpm start"
